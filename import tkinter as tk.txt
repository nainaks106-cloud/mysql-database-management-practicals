import tkinter as tk

# Main window
root = tk.Tk()
root.title("Login Form")
root.geometry("300x200")

# Correct credentials (you can change these)
correct_username = "admin"
correct_password = "1234"

# Function to check login
def login():
    username = entry_username.get()
    password = entry_password.get()

    if username == correct_username and password == correct_password:
        result_label.config(text="Login Successful ✅", fg="green")
    else:
        result_label.config(text="Login Failed ❌", fg="red")

# Username label & entry
tk.Label(root, text="Username").pack(pady=5)
entry_username = tk.Entry(root)
entry_username.pack(pady=5)

# Password label & entry
tk.Label(root, text="Password").pack(pady=5)
entry_password = tk.Entry(root, show="*")
entry_password.pack(pady=5)

# Login button
tk.Button(root, text="Login", command=login).pack(pady=10)

# Result label
result_label = tk.Label(root, text="")
result_label.pack()

# Run app
root.mainloop()


import tkinter as tk
root = tk.Tk()
var = tk.IntVar()
check = tk.Checkbutton(root, text = "accept", variable=var)
check.pack()
def show():
    print(var.get())
btn = tk.Button(root,text = "Check value", command=show)
btn.pack()
root.mainloop()

import tkinter as tk
root = tk.Tk()
var = tk.StringVar()
tk.Radiobutton(root, text = "Male", variable = var, value ="Male").pack()
tk.Radiobutton(root, text ="Female", variable=var, value ="Female").pack()
def show():
    print(var.get())
tk.Button(root, text="show selection", command = show).pack()
root.mainloop()

import tkinter as tk
root= tk.Tk()
listbox = tk.Listbox(root)
listbox.pack()
listbox.insert(1, "Python")
listbox.insert(2, "Java")
listbox.insert(3, "C++")
def show():
    print(listbox.get(listbox.curselection()))
tk.Button(root, text ="Show Selected ", command = show).pack()
root.mainloop()import tkinter as tk
root= tk.Tk()
listbox = tk.Listbox(root)
listbox.pack()
listbox.insert(1, "Python")
listbox.insert(2, "Java")
listbox.insert(3, "C++")
def show():
    print(listbox.get(listbox.curselection()))
tk.Button(root, text ="Show Selected ", command = show).pack()
root.mainloop()